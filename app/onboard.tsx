import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const onboard = () => {
    return (
        <View className="onboard flex-1 min-h-screen justify-center items-center">
            <Text>onBoard</Text>
        </View>
    );
};

export default onboard;

const styles = StyleSheet.create({});
