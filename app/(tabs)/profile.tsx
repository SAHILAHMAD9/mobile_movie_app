import React from 'react';
import { View, Text, StyleSheet } from 'react-native';

const profile = () => {
    return (
        <View>
            <Text> sahil profile $COMPONENT$</Text>
        </View>
    );
};

export default profile;

const styles = StyleSheet.create({});
